#include "livre.h"

livre::livre(QString("livre")) : attributes({persistentAttribute auteur,
                                            persistentAttribute titre,
                                            persistentAttribute isbn,
                                            persistentAttribute annee})

{

}
